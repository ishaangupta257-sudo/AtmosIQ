{
  "name": "atmosiq-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "AtmosIQ Node/Express REST API — orchestrates the Python ML engine and serves the React frontend.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongodb": "^6.8.0",
    "node-cron": "^3.0.3"
  }
}
